#!/usr/bin/env bun

import { runBrainlayerProxyDaemon } from "../src/brainlayer-proxy/cli";

runBrainlayerProxyDaemon().catch((err) => {
  console.error(
    `BRAINLAYER_PROXY_FATAL ${err instanceof Error ? err.message : String(err)}`,
  );
  process.exit(1);
});
